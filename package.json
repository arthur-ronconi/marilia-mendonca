{
  "name": "marilia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "push": "git push && git checkout main && git merge develop && git push && git checkout develop"
  },
  "dependencies": {
    "bootstrap": "^4.5.3",
    "next": "^13.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.27.0"
  }
}
